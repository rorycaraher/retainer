# Ideas (future / out of scope for v1)

Things deliberately deferred during initial design, kept here so they aren't forgotten.

- **Nested checklist items** — sub-items under a parent Checklist Item. Out of scope for v1 — it would complicate the flat item-level sync/merge model, but could be worth revisiting later.
